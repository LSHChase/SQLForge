<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 配置项 mapper：说明该配置的用途、默认值或运行影响。 -->
<mapper namespace="com.company.governance.infrastructure.persistence.mapper.ReportInterfaceConfigMapper">

    <!-- 配置项 resultMap id=ReportInterfaceConfigResultMap：说明该配置的用途、默认值或运行影响。 -->
    <resultMap id="ReportInterfaceConfigResultMap" type="com.company.governance.infrastructure.persistence.entity.ReportInterfaceConfigRecord">
        <!-- 配置项 id：说明该配置的用途、默认值或运行影响。 -->
        <id property="configId" column="config_id"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="tenantId" column="tenant_id"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="datasourceCode" column="datasource_code"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="stage" column="stage"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="sourceType" column="source_type"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="endpointCode" column="endpoint_code"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="endpointName" column="endpoint_name"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="baseUrl" column="base_url"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="pathTemplate" column="path_template"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="httpMethod" column="http_method"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="reportCodeParamName" column="report_code_param_name"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="sqlJsonPath" column="sql_json_path"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="authMode" column="auth_mode"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="timeoutMs" column="timeout_ms"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="enabled" column="enabled"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="createTime" column="create_time"/>
        <!-- 配置项 result：说明该配置的用途、默认值或运行影响。 -->
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 配置项 sql id=BaseColumns：说明该配置的用途、默认值或运行影响。 -->
    <sql id="BaseColumns">
        config_id,
        tenant_id,
        datasource_code,
        stage,
        source_type,
        endpoint_code,
        endpoint_name,
        base_url,
        path_template,
        http_method,
        report_code_param_name,
        sql_json_path,
        auth_mode,
        timeout_ms,
        enabled,
        create_time,
        update_time
    </sql>

    <!-- 配置项 insert id=upsert：说明该配置的用途、默认值或运行影响。 -->
    <insert id="upsert" parameterType="com.company.governance.infrastructure.persistence.entity.ReportInterfaceConfigRecord">
        INSERT INTO report_interface_config (
            config_id,
            tenant_id,
            datasource_code,
            stage,
            source_type,
            endpoint_code,
            endpoint_name,
            base_url,
            path_template,
            http_method,
            report_code_param_name,
            sql_json_path,
            auth_mode,
            timeout_ms,
            enabled,
            create_time,
            update_time
        ) VALUES (
            #{configId},
            #{tenantId},
            #{datasourceCode},
            #{stage},
            #{sourceType},
            #{endpointCode},
            #{endpointName},
            #{baseUrl},
            #{pathTemplate},
            #{httpMethod},
            #{reportCodeParamName},
            #{sqlJsonPath},
            #{authMode},
            #{timeoutMs},
            #{enabled},
            #{createTime},
            #{updateTime}
        )
        ON DUPLICATE KEY UPDATE
            datasource_code = VALUES(datasource_code),
            stage = VALUES(stage),
            source_type = VALUES(source_type),
            endpoint_code = VALUES(endpoint_code),
            endpoint_name = VALUES(endpoint_name),
            base_url = VALUES(base_url),
            path_template = VALUES(path_template),
            http_method = VALUES(http_method),
            report_code_param_name = VALUES(report_code_param_name),
            sql_json_path = VALUES(sql_json_path),
            auth_mode = VALUES(auth_mode),
            timeout_ms = VALUES(timeout_ms),
            enabled = VALUES(enabled),
            update_time = VALUES(update_time)
    </insert>

    <!-- 配置项 select id=selectByTenantIdAndConfigId：说明该配置的用途、默认值或运行影响。 -->
    <select id="selectByTenantIdAndConfigId" resultMap="ReportInterfaceConfigResultMap">
        SELECT
        <!-- 配置项 include：说明该配置的用途、默认值或运行影响。 -->
        <include refid="BaseColumns"/>
        FROM report_interface_config
        WHERE tenant_id = #{tenantId}
          AND config_id = #{configId}
    </select>

    <!-- 配置项 select id=selectByTenantId：说明该配置的用途、默认值或运行影响。 -->
    <select id="selectByTenantId" resultMap="ReportInterfaceConfigResultMap">
        SELECT
        <!-- 配置项 include：说明该配置的用途、默认值或运行影响。 -->
        <include refid="BaseColumns"/>
        FROM report_interface_config
        WHERE tenant_id = #{tenantId}
        ORDER BY endpoint_code ASC, update_time DESC
    </select>

    <!-- 配置项 select id=selectMatchCandidates：说明该配置的用途、默认值或运行影响。 -->
    <select id="selectMatchCandidates" resultMap="ReportInterfaceConfigResultMap">
        SELECT
        <!-- 配置项 include：说明该配置的用途、默认值或运行影响。 -->
        <include refid="BaseColumns"/>
        FROM report_interface_config
        WHERE tenant_id = #{tenantId}
          AND (datasource_code IS NULL OR datasource_code = '' OR datasource_code = #{datasourceCode})
          AND (stage IS NULL OR stage = '' OR stage = #{stage})
        ORDER BY
          (CASE WHEN datasource_code IS NULL OR datasource_code = '' THEN 0 ELSE 2 END
           + CASE WHEN stage IS NULL OR stage = '' THEN 0 ELSE 1 END) DESC,
          update_time DESC
    </select>
</mapper>
