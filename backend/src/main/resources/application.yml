server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  mvc:
    problemdetails:
      enabled: true

sqlforge:
  cors:
    allowed-origin: http://localhost:5173
  storage:
    connections-file: ${java.io.tmpdir}/sqlforge/connections.json
