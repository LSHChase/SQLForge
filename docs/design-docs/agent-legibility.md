# Agent Legibility

SQLForge should be easy for future agents to extend without re-discovering hidden intent.

We optimize for:

- obvious domain ownership
- shallow entry points
- durable docs with cross-links
- deterministic examples and tests

We avoid:

- giant instruction files
- hidden product rules in chat only
- ambiguous dependency directions
- connector-specific behavior mixed directly into workflows
