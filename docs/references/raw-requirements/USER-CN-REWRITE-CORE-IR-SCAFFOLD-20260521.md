# USER-CN-REWRITE-CORE-IR-SCAFFOLD-20260521 原始需求

收到时间：2026-05-21

```text
接下来要提升改写的能力，不动页面，改写核心架构如下，本次先搭架子，不要有缺失，如有冲突要给出来选择。核心架构：┌─────────────────────────────────────────┐
│  L5: 业务意图层 (Business Intent)        │  ← "百万客户净增报表" = 时间窗口对比 + 客户分层
│     模式: {时间锚点, 度量, 维度, 筛选}     │
├─────────────────────────────────────────┤
│  L4: 关系代数层 (Relational Algebra)     │  ← 投影/选择/聚合/连接/并集的标准形
│     模式: σ, π, γ, ⋈, ∪, ∩, -           │
├─────────────────────────────────────────┤
│  L3: 查询块层 (Query Block)              │  ← 子查询单元、CTE、视图展开
│     模式: {输入, 输出, 谓词, 聚合}        │
├─────────────────────────────────────────┤
│  L2: 表引用层 (Table Reference)           │  ← 物理表、派生表、关联路径
│     模式: {源, 别名, 访问路径, 谓词下推}   │
├─────────────────────────────────────────┤
│  L1: 语法树层 (AST)                      │  ← Calcite SqlNode / JSqlParser Expression
│     模式: 具体方言节点                    │
└─────────────────────────────────────────┘
```

执行约束：

- 不动页面。
- 本次先搭改写核心架构骨架。
- 五层骨架不得缺失。
- 若与现有系统冲突，需要给出可选方案。
