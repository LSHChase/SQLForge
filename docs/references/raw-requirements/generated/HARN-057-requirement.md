用户在 HARN-056 完成后要求对解析历史与批量解析相关内容进行校验核对和优化：

1. 解析历史页面刷新不出来，需要修复刷新路径。
2. 解析历史页面默认查询条件都应该为空。
3. 对上一轮“所有 SQL 解析记录落库、批量解析中心、文件导入解析、统计展示和历史展示”的结果继续核对优化。

执行边界：

- 本任务作为 HARN-056 follow-up，不改写 HARN-056 已归档结论。
- 优先修复解析历史页筛选默认值、刷新请求上下文与批量历史刷新联动。
- 不重写核心 SQL parser 算法，不改变后端租户隔离与历史持久化语义。
- 输出物必须包含代码、测试和文档。
