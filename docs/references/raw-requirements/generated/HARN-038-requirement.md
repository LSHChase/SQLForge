治理需求：在 A-STORY-007 下新增 formal task HARN-038，用于修复 governed closeout / post-closeout / healthcheck 的真实治理缺口。 目标范围：1. 修复 closeout post-check 中调用 python3 scripts/governed_healthcheck.py --check 时，对当前刚归档任务的 post-closeout actual evidence 产生自引用检查，导致 closeout 在 commit 已成功后仍被误判失败。2. 补齐 closeout 成功提交后若 post-check 失败时的 runtime 收尾语义，避免 current-task/session-context 长时间停留在 closeout 中间态；需要提供严格但可恢复的标准路径，而不是手工拼接修复。3. 保持 governed_healthcheck 对 implementation 期间 tracked dirty worktree 的严格阻断，不得为了通过 post-closeout 检查而削弱该边界。4. 对齐 docs/operations/requirements-to-task-playbook.md 以及相关运行时/治理文档，明确 healthcheck 在 pre-confirm、post-closeout、runtime repair 三种场景下的边界和推荐用法。 治理边界：不得削弱 Main Foreman 唯一 write-back / validate / closeout 入口；不得把失败的 post-closeout actual evidence 静默抹掉；不得跳过 task_audit；不得引入第二套长期真值。 输出物：脚本、运行时治理修复、文档、自动化验证。 限制：严格模式；不修改业务代码；不引入破坏性 git 命令；不要通过放宽 healthcheck 规则来掩盖 closeout 缺陷。
